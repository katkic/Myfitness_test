<div class="container mt-4">
  <h1>グラフ</h1>

  <div class="mt-3">
    <h2>体重</h2>
      <%= line_chart BodyStatus.where(user_id: current_user.id).pluck(:created_at, :body_weight) %>
  </div>

  <div class="mt-3">
    <h2>体脂肪</h2>
      <%= line_chart BodyStatus.where(user_id: current_user.id).pluck(:created_at, :body_fat), colors: ['#b00'] %>
  </div>
</div><!-- /.container -->
